apiVersion: v1
kind: Service
metadata:
  namespace: customer
  name: customer-db
  labels:
    type: service
spec:
  selector:
    app: customer
    type: api
    mode: db
  ports:
    - name: main
      protocol: TCP
      port: 80
      targetPort: 8080
