apiVersion: v1
kind: PersistentVolume
metadata:
  namespace: customer
  name: init
  labels:
    app: postgres
    usage: init
spec:
  capacity:
    storage: 200Mi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  storageClassName: init
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /init/customer
    type: DirectoryOrCreate
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: customer
  name: init-claim
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 200Mi
    limits:
      storage: 500Mi
  storageClassName: init
  selector:
    matchLabels:
      app: postgres
      usage: init
---
apiVersion: batch/v1
kind: Job
metadata:
  namespace: customer
  name: seed
spec:
  template:
    metadata:
      namespace: customer
      labels:
        type: seeder
    spec:
      volumes:
        - name: init
          persistentVolumeClaim:
            claimName: init-claim 
      containers:
        - name: seeder
          image: vmerv/analytics-seeder-customer
          env:
            - name: OUTPUT_MEDIUM
              value: FILE
            - name: OUTPUT_DESTINATION
              value: /init
            - name: ROWS_NUMBER
              value: "customers:100000,transactions:400000"
          volumeMounts:
            - name: init
              mountPath: /init
      restartPolicy: Never
  backoffLimit: 4
